- name: bringup on dev_mote_v1_0 - Debug (w/ bootloader)
  type: build_fw
  args:
    app: bringup
    bsp: dev_mote_v1_0
    build_type: Debug
    use_bootloader: True

- name: bringup_bristlefin on dev_mote_bristlefin_v1_0 - Debug (w/ bootloader)
  type: build_fw
  args:
    app: bringup_bristlefin
    bsp: dev_mote_bristlefin_v1_0
    build_type: Debug
    use_bootloader: True

- name: bringup_bridge on bridge_v1_0 - Debug (w/ bootloader)
  type: build_fw
  args:
    app: bringup_bridge
    bsp: bridge_v1_0
    build_type: Debug
    use_bootloader: True

- name: bringup_mote on bm_mote_v1.0 - Debug (w/ bootloader)
  type: build_fw
  args:
    app: bringup_mote
    bsp: bm_mote_v1.0
    build_type: Debug
    use_bootloader: True